"""JARVIS Humanoid Robot Brain — robotics sub-package.

Pass 19: simulation bridge, motion skills, NLP→motion, STT, RL, vision,
and the master RobotBrain coordinator.

All hardware dependencies (pybullet, mujoco, whisper, ultralytics, torch,
gymnasium) are optional — MOCK backends require zero external packages.
"""
